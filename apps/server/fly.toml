# Fly.io configuration for Interval API Server
# https://fly.io/docs/reference/configuration/

app = "interval"
primary_region = "nrt"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  PORT = "3000"

[deploy]
  strategy = "rolling"

# HTTP service configuration
[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

  # Concurrency limits
  [http_service.concurrency]
    type = "requests"
    hard_limit = 250
    soft_limit = 200

# Health check configuration
[checks]
  [checks.health]
    grace_period = "60s"
    interval = "30s"
    method = "GET"
    path = "/api/health"
    port = 3000
    timeout = "10s"
    type = "http"

# Machine configuration (cost-effective)
[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
